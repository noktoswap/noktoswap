{
  "name": "xmrp2p-subgraph",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy xmrp-2-p",
    "create-local": "graph create --node http://localhost:8020/ xmrp-2-p",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 xmrp-2-p",
    "abi": "cd ../contracts && forge inspect XMRP2P abi --json > ../subgraph/abis/XMRP2P.json"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.97.0",
    "@graphprotocol/graph-ts": "^0.38.0"
  }
}
